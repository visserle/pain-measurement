<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Willkommen!</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
    <link rel="icon" type="image/x-icon" href="{{ url_for('static', filename='favicon.ico') }}">
</head>
<body>
    <h1>{{ title }}</h1>
    <p>{{ introduction }}</p>
    <p>{{ privacy_info }}</p>    <p>{{ instructions }}</p>
    <p>{{ hydration_info }}</p>
    <p>{{ questionnaire_info }}</p>
    <a href="{{ url_for('questionnaire_handler', scale='general') }}" class="start-button">Start Survey</a>
</body>
</html>
