<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
    <style>
        .question {
            margin-bottom: 20px;
        }
        .question p {
            margin-bottom: 5px;
        }
        .question label {
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{ title }}</h1>
        <p>{{ instructions }}</p>
        <form method="post">
            {% for question in questions %}
            <div class="question">
                <p><strong>{{ question.id }}:</strong> {{ question.text }}</p>
                <br>
                <div>
                    fast nie  <!-- almost never -->
                    {% for i in options %}
                    <input type="radio" id="q{{ question.id }}_option{{ i.score }}" name="q{{ question.id }}" value="{{ i.score }}" required>
                    <label for="q{{ question.id }}_option{{ i.score }}">{{ i.score }}</label>
                    {% endfor %}
                    fast immer  <!-- almost always -->
                </div>
                <br>
            </div>
            {% endfor %}
            <button type="submit">Abschicken</button>
        </form>
    </div>
</body>
</html>
